# Annotation Standard

This project uses a structured annotation vocabulary
to document decisions, constraints, and alternatives
directly alongside code and configuration.

The annotation standard improves explainability
without enforcing semantics or policy.
